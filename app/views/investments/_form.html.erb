<%= form_for(@investment) do |f| %>
  <% if @investment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@investment.errors.count, "error") %> prohibited this investment from being saved:</h2>

      <ul>
      <% @investment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select :investor_id, Investor.all.collect { |i| [ i.full_name, i.id ] } %>
  </div>
  <div class="field">
    <%= f.select :fund_id, Fund.all.collect { |fund| [ fund.name, fund.id ] } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
